$('#payment_detail').html('Payment Details display here').show();
$('.close_option[field="membership_fee"]').click();
$('#existing_membership_fees').html('<%= escape_javascript(render(:partial => "/membership_fees/existing_membership_fee")) %>');
enable_form_after_submit_finish();
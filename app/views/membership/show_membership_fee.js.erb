
$('#membership_content').html('<%= escape_javascript(render(:partial => "/membership_fees/exsiting_membership_fee")) %>');
$('#membership_fee_container').dialog({
  modal: true,
  resizable: true,
  draggable: true,
  position: "center",
  width: 680,
  height: 600,
  buttons: {
  }
});
$('#membership_fee_container').dialog("option","title","Membership Fee");
$('#membership_fee_container').dialog('open');
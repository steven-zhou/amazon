<% if @note.errors.empty? -%>
  $('#notes_list').prepend('<%= escape_javascript(render(:partial => @note)) %>');
  $("#new_note")[0].reset();
  $("#new_note").toggle('blind');
  $('#<%= dom_id(@note) %>').effect('slide', {direction:'right'});  
<% end -%>
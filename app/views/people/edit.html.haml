#left
  .header
    .title
      = render :partial => "menu", :locals => { :person => @person }
    =link_to image_tag 'help_icon.png', :style => 'float:right'
    %br.clear
  
  - form_tag(:controller => "list", :action => "show", :id => "") do
    %fieldset{:style => 'text-align:center'}
      = link_to image_tag 'back.png'
      = link_to image_tag 'back.png'
      = select_tag 'list', :name, :style => 'width:200px;'
      = link_to image_tag 'go.png'
      = link_to image_tag 'go.png'

  - form_for @person, :html=>{:multipart => true, :id=>"person_names_form"} do |p|
    %fieldset
      %h3 Name Details
      = render :partial => 'person_name_fields', :locals => {:p => p}
    %fieldset
      %h3 Personal Details
      = render :partial => 'person_details_fields', :locals => {:p => p}
    %fieldset
      = render :partial => '/images/show_photo', :locals => {:person => @person}
    = submit_tag 'Save and Close', :style => 'float:right;'
    = submit_tag 'Save', :style => 'float:right;'
    

#right
  #tabs
    = render :partial => 'person_menu'
    #Address
      = render :partial => "/addresses/addresses_tab",:locals =>{:person => @person}
    #Contact
      = render :partial => "/contacts/contacts_tab", :locals => {:person => @person}
    #Keyword
      = render :partial => "/keywords/keywords_tab",:locals =>{:person => @person}
    #MasterDocs
      = render :partial => "/master_docs/masterdocs_tab", :locals =>{:person => @person}
    #Rels
      = render :partial => "/relationships/relationships_tab", :locals  => {:person  => @person}
    #Note
      = render :partial => "/notes/notes_tab",:locals =>{:person => @person}
    #Role
      = render :partial => "/roles/roles_tab",:locals =>{:person => @person}    
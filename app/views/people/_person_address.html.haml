-# Displays An Address
-div_for(person_address, :class => 'info_container') do
  .header_container
    .title
      %h3= person_address.address_type.name
    
    %br{ :style => "clear:both;"}
  .display_container
    = format_address(person_address)
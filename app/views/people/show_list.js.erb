$('#show_list_content').html('<%= escape_javascript(render(:partial => "/people/search_list_results", :locals =>{:people => @p, :current_operation => @current_operation,:active_tab => @active_tab,:active_sub_tab => @active_sub_tab })) %>');
$('#show_list_grid').dialog({
  modal: true,
  resizable: true,
  draggable: true,
  position: "center",
  width: 680,
  height: 600,
  buttons: {
  }
});
$('#show_list_grid').dialog("option","title","Show List");
$('#show_list_grid').dialog('open');

$('#show_all_list_member_disabled').attr("id","show_all_list_member");


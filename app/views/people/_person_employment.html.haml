-# Displays Employment
-div_for(person_employment, :class => 'header_container') do
  %div
    .title
      %h2= "#{person_employment.organisation.full_name}"
    
  %div{:style => "float:right;"}
    = format_employment(person_employment)
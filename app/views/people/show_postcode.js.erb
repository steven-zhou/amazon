$('#postcode_grid').html('<%= escape_javascript(render(:partial => "postcodes/postcode_assistant")) %>')
$('#address_form_assistant').dialog({
            modal: true,
            resizable: true,
            draggable: true,
            width: 550,
            height: 550,
            buttons: {
        }
        });

        $('#address_form_assistant').dialog('option','title','Postcode');
        $('#address_form_assistant').dialog('open');
        

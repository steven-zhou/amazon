$('#add_new_source').html('<%= escape_javascript(render(:partial => "/receipting/new_source")) %>');
$('#existing_sources').html('<%= escape_javascript(render(:partial => "/receipting/existing_sources")) %>');
$('#source_mode').attr('mode', 'show');
%thead
  %tr
    %th ID
    %th Name
    %th Email
    %th Phone
    %th Website
    %th Fax

%tbody
  - @report_list.people_on_list.each do |person|
    %tr{:person_id => person.id}
      %td= person.id
      %td= person.name
      %td= person.primary_email.value unless person.primary_email.nil?
      %td= person.primary_phone.complete_number unless person.primary_phone.nil?
      %td= person.websites.first.value unless person.websites.empty?
      %td= person.primary_fax.complete_number unless person.primary_fax.nil?



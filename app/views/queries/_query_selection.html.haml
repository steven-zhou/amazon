-# Displays An Query Criteria
-div_for(query_selection, :class => 'info_container') do
  .header_container
    .title
      %h3= query_selection.formatted_info
    .options
      = link_to image_tag('delete_icon.png'), query_selection_path(query_selection), :class => 'delete'
    %br.clear
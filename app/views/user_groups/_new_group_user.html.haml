- status = login_account.login_status ? "container_active" : "container_inactive"
-div_for(login_account, :class => "container_active half_container toggle_options" , :id => "existing_user_in_group", :login_account_id => login_account.id, :field => "user_group") do
  .options{:id => "delete_icon"}
    = link_to image_tag('delete_icon.png'), user_group_path(@user_group), :class => 'delete delete_flag',:id => 'delete_user_group'
  .title
    = login_account.person_id
    \-
    = login_account.user_name
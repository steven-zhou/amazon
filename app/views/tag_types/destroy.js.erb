$(".show_tag_types[tag_meta_types_id = " + "<%= escape_javascript("#{@tag_meta_type.id}")%>" + "]").click();

$(".show_tag_types[tag_meta_types_id = " + "<%= escape_javascript("#{@tag_meta_type.id}")%>" + "]").click();

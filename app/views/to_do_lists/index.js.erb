$('#manage_to_do_dialog').html('<%= escape_javascript(render(:partial => "/to_do_lists/to_do_list_manager")) %>');
$('#manage_to_do_dialog').dialog( {
  modal: true,
    resizable: false,
  width: 800,
  height: 600,
  draggable: true
});
$('#manage_to_do_dialog').dialog('option', 'title', 'To Do Manager');
$('#manage_to_do_dialog').dialog('open');

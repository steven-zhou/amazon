$('#to_do_detail_<%= escape_javascript("#{@to_do_list.id}") %>').fadeOut("slow", function() {$(this).remove();});
$('#to_do_lists').html('<%= escape_javascript(render(:partial => "to_do_lists/to_do_list")) %>');
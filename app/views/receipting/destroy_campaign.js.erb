  $('#edit_campaign').hide();
  $('#add_new_campaign').html('<%= escape_javascript(render(:partial => "/receipting/new_campaign")) %>');
  $('#add_new_campaign').show();
  $('#existing_campaigns').html('<%= escape_javascript(render(:partial => "/receipting/existing_campaigns")) %>');
  $('#campaign_mode').attr('mode', 'show');
    $("#show_campaigns_grid").flexigrid({
        url: '/grids/show_campaigns_grid',
        dataType: 'json',
  colModel : [
  {
  display: 'ID',
  name : 'id',
  width : 40,
  sortable : true,
  align: 'left'
  },

  {
  display: 'Name',
  name : 'name',
  width : 100,
  sortable : true,
  align: 'left'
  },

  {
  display: 'Target Amount',
  name : 'target_amount',
  width : 80,
  sortable : true,
  align: 'left'
  },

  {
  display: 'Start Date',
  name : 'start_date',
  width : 100,
  sortable : true,
  align: 'left'
  },

  {
  display: 'End Date',
  name : 'end_date',
  width : 100,
  sortable : true,
  align: 'left'
  },

  {
  display: 'Description',
  name : 'description',
  width : 60,
  sortable : true,
  align: 'left'
  }

  ],
  searchitems : [
  {
  display: 'Name',
  name : 'Name'
  },

  {
  display: 'Start Date',
  name : 'start_date'
  },

  {
  display: 'End Date',
  name : 'end_date'
  },

  {
  display: 'Target Amount',
  name : 'target_amount'
  }

  ],
  sortname: "start_date, name",
  sortorder: "asc",
  usepager: true,
  title: 'Campaigns',
  useRp: true,
  rp: 20,
  showTableToggleBtn: false,
  width: 'auto',
  height: 'auto'
  });

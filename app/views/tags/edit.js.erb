$('#tag_form').html("<%= escape_javascript(render(:partial => '/tag_settings/tag')) %>");
$('#tag_form').dialog( {
        modal: true,
        resizable: true,
        draggable :true});
$('#tag_form').dialog('option', 'title', 'Edit');
$('#tag_form').dialog('open')
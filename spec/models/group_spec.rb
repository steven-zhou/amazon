require File.expand_path(File.dirname(__FILE__) + '/../spec_helper')

describe  Group  do
#
#  it { should belong_to(:group_owner)}
#  it { should belong_to(:group_type)}
#
#  before(:each) do
#    @primary_list = Factory(:primary_list)
#    @group = Factory.build(:group)
#  end
#
#  it "should not save when the name already in the database when the group type is the same" do
#
#    @group_1 = Factory(:group)
#    @group_2 = Group.new(:tags_id => @group_1.tags_id, :name => @group_1.name)
#    @group_2.save.should == false
#  end
#
#  it "should save when the name is the same but the group type is different" do
#    @group_1 = Factory(:group)
#    @group_2 = Group.new(:tags_id => "3", :name => @group_1.name)
#    @group_2.save.should == true
#
#  end
#
#  it "should not save when the name is blank" do
#     @group.name = ""
#     @group.save.should == false
#  end



  
end